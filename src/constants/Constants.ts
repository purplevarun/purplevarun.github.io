const TRANSITION = '1.5s'
const BLACK = 'black'
const WHITE = 'white'
const NAME_DELAY = 1
const BTN_DELAY = NAME_DELAY + 1
const ICON_SIZE = 40

export { TRANSITION, BLACK, WHITE, NAME_DELAY, BTN_DELAY, ICON_SIZE }
